#include <stdio.h>
char* removeDuplicates(char* s) {
    int i = 0; 
    for (int j = 0; s[j] != '\0'; j++) {
        if (i > 0 && s[j] == s[i - 1]) {
            i--; 
        } else {
            s[i] = s[j]; 
            i++;
        }
    }
    s[i] = '\0'; 
    return s;
}